extends layout.pug

block content
    .container
        .column
            .row
                h1 Server is running
            .row
                p Environment 
                    em #{environment}